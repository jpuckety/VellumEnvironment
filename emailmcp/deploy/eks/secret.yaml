apiVersion: v1
kind: Secret
metadata:
  name: emailmcp-secrets
  namespace: emailmcp
type: Opaque
data:
  # Base64 encoded 32-byte key
  # echo -n "your-key" | base64
  EMAILMCP_MASTER_KEY: "${EMAILMCP_MASTER_KEY_B64}"
